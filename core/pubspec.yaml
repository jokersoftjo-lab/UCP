name: ucp_core
description: "UCP Core Protocol v1.0: platform-independent universal control protocol models and contracts."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.13.3
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
